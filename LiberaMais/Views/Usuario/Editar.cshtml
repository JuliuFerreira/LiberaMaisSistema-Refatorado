@using LiberaMais.Models.Enums

@model UsuarioModel

@{
    Layout = "LayoutSistema";
}

<body>

    

    <form style="border: solid 1px black; padding:10px; border-radius: 10px" id="usuarioForm" asp-controller="Usuario" asp-action="Editar" method="post" class="row g-3">

        <input type="hidden" asp-for="Id" />

        <h2 style="text-align:center; margin-top: 50px; margin-bottom: 50px">ALTERAR USUÁRIO</h2>

        <div class="col-md-2">
            <label for="nome" class="form-label">Nome</label>
            <input type="text" asp-for="Nome" class="form-control" id="nome">
            @Html.ValidationMessageFor(x => x.Nome)
        </div>

        <div class="col-md-2">
            <label for="email" class="form-label">email</label>
            <input type="text" asp-for="Email" class="form-control" id="email">
            @Html.ValidationMessageFor(x => x.Email)
        </div>

        <div class="col-md-2">
            <label for="login" class="form-label">Login</label>
            <input type="text" asp-for="Login" class="form-control" id="login">
            @Html.ValidationMessageFor(x => x.Login)
        </div>

        <div class="col-md-2">
            <label for="perfil" class="form-label">Perfil</label>
            <select asp-for="Perfil" class="form-control">
                <option value="">Selecione</option>
                <option value="@PerfilEnum.Admin">Administrador</option>
                <option value="@PerfilEnum.Padrao">Padrão</option>
            </select>
            @Html.ValidationMessageFor(x => x.Perfil)
        </div>

        <div class="col-12">
            <button type="submit" class="btn btn-outline-success" onclick="return validarForm();">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-floppy2-fill" viewBox="0 0 16 16">
                    <path d="M12 2h-2v3h2z" />
                    <path d="M1.5 0A1.5 1.5 0 0 0 0 1.5v13A1.5 1.5 0 0 0 1.5 16h13a1.5 1.5 0 0 0 1.5-1.5V2.914a1.5 1.5 0 0 0-.44-1.06L14.147.439A1.5 1.5 0 0 0 13.086 0zM4 6a1 1 0 0 1-1-1V1h10v4a1 1 0 0 1-1 1zM3 9h10a1 1 0 0 1 1 1v5H2v-5a1 1 0 0 1 1-1" />
                </svg>
                Gravar
            </button>

            <a type="button" class="btn btn-outline-danger" asp-controller="Usuario" asp-action="Index">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-x-circle-fill" viewBox="0 0 16 16">
                    <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0M5.354 4.646a.5.5 0 1 0-.708.708L7.293 8l-2.647 2.646a.5.5 0 0 0 .708.708L8 8.707l2.646 2.647a.5.5 0 0 0 .708-.708L8.707 8l2.647-2.646a.5.5 0 0 0-.708-.708L8 7.293z" />
                </svg>
                Cancelar
            </a>
        </div>
    </form>

    <script>
        function validarForm() {
            var nomeInput = document.getElementById('nome').value;

            // Verifica se o nome contém um espaço, indicando nome e sobrenome
            if (nomeInput.indexOf(' ') === -1) {
                document.getElementById('nomeError').style.display = 'block'; // Exibe a mensagem de erro
                return false; // Impede o envio do formulário
            } else {
                document.getElementById('nomeError').style.display = 'none'; // Oculta a mensagem de erro
                return true; // Permite o envio do formulário
            }
        }
    </script>

    <script>
        // Seleciona o campo de entrada
        var inputNome = document.getElementById('nome');

        // Adiciona um ouvinte de evento de entrada para o campo de entrada
        inputNome.addEventListener('input', function () {
            // Converte o valor para maiúsculas
            this.value = this.value.toUpperCase();
        });
    </script>

    <script>
        function voltar() {
            window.history.back();
        }
    </script>

    @section Scripts {
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.all.min.js"></script>
        <script type="text/javascript">
            $(document).ready(function () {
                $("form#usuarioForm").submit(function (event) {
                    event.preventDefault(); // Impede o envio do formulário padrão

                    var form = $(this);
                    $.ajax({
                        type: form.attr('method'),
                        url: form.attr('action'),
                        data: form.serialize(),
                        success: function (response) {
                            if (response.success) {
                                // Mostra o diálogo de sucesso com SweetAlert2
                                Swal.fire({
                                    title: 'Alteração realizada com sucesso!',
                                    text: 'Redirecionando...',
                                    icon: 'success',
                                    timer: 3000, // 3 segundos
                                    showConfirmButton: false
                                }).then(() => {
                                    // Redireciona para a view Clientes após 3 segundos
                                    window.location.href = "@Url.Action("Index", "Usuario")";
                                });
                            } else {
                                Swal.fire({
                                    title: 'Erro',
                                    text: response.message,
                                    icon: 'error'
                                });
                            }
                        },
                        error: function () {
                            Swal.fire({
                                title: 'Erro',
                                text: 'Ops, ocorreu um erro. Tente novamente.',
                                icon: 'error'
                            });
                        }
                    });
                });
            });
        </script>
    }

</body>